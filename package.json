{
  "name": "garage_crawler",
  "private": "true",
  "scripts": {
    "product": "pnpm --filter product_to_engine ts-node",
    "vin": "pnpm --filter vin_to_product ts-node"
  },
  "dependencies": {
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  }
}
